//! Completed report formats with isolated presentation dependencies.

/// Human rendering with an explicitly selected visual theme.
pub mod human;
/// Theme-independent serialization of the versioned JSON document.
pub mod json;
